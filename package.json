{
  "name": "manjaro-media1",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "encrypt:preview": "staticrypt dist/preview -r -d dist --short --remember 30",
    "build:full": "npm run build && npm run encrypt:preview",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4"
  }
}
